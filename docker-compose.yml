services:
  dashboard:
    build: .
    container_name: sp-dashboard
    restart: unless-stopped
    ports:
      - "127.0.0.1:5000:5000"
    env_file: .env
    volumes:
      - ./credentials.json:/app/credentials.json:ro